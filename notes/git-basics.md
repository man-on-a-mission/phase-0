Today I have learned why we use >> and not > if we do not which to overwrite the content of a while we are adding to.
 I have learn properly the concept of JS being a single threaded language, and how the event loop is execute.
 I have also learn how does async/await help us execute other queued events avoiding events that need time to be executed from blocking the loop.
I learned the real meaning of origin (nickname for the remote repo url), HEAD (pointer to the current position in history, meaning which commit you are on now), and main which is just the branch name.
 Learned what does Detached HEAD state means, which basically happens when you move back to an older commit, instead of working on the most recent/current one.
